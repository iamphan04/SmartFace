{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "python -m pip install --disable-pip-version-check -r database_pdt/requirements.txt && npm --prefix frontend ci && npm --prefix frontend run build"
  },
  "deploy": {
    "startCommand": "python database_pdt/main.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
